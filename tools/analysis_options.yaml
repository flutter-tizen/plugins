include: ../analysis_options.yaml

analyzer:
  exclude:
    - 'build/**'
    - 'android/**'
    - 'ios/**'
    - 'web/**'
    - 'windows/**'
    - 'macos/**'
    - 'linux/**'

linter:
  rules:
    avoid_print: false
    implementation_imports: false
