name: firebase_core_tizen_example
description: Demonstrates how to use the firebase_core_tizen plugin.

publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  firebase_core: ^1.24.0
  firebase_core_tizen:
    path: ../
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
