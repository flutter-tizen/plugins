name: flutter_local_notifications_tizen
description: A new flutter plugin project.
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^9.1.5

flutter:
  plugin:
    platforms:
      tizen:
        pluginClass: FlutterLocalNotificationsPlugin
        fileName: flutter_local_notifications_plugin.h
