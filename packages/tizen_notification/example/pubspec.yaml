name: tizen_notification_example
description: Demonstrates how to use the tizen_notification plugin.
publish_to: "none"

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  tizen_notification:
    path: ../

flutter:
  uses-material-design: true
