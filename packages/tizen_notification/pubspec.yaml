name: tizen_notification
description: Tizen notification APIs. Used to show and delete notifications on a Tizen device.
homepage: https://github.com/flutter-tizen/plugins
repository: https://github.com/flutter-tizen/plugins/tree/main/packages/tizen_notification
version: 0.2.2

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  tizen_app_control: ^0.2.2

flutter:
  plugin:
    platforms:
      tizen:
        pluginClass: TizenNotificationPlugin
        fileName: tizen_notification_plugin.h
