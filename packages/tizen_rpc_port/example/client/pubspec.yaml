name: tizen_rpc_port_client_example
description: Demonstrates how to use the tizen_rpc_port client API.
publish_to: "none"

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  tizen_rpc_port:
    path: ../../

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  integration_test_tizen:
    path: ../../../integration_test/

flutter:
  uses-material-design: true
