name: wakelock_plus_tizen_example
description: Demonstrates how to use the wakelock_plus_tizen plugin.
publish_to: "none"

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  wakelock_plus: ^1.6.0
  wakelock_plus_tizen:
    path: ../

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  integration_test_tizen:
    path: ../../integration_test/
