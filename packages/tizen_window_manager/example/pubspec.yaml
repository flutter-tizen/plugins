name: tizen_window_manager_example
description: Demonstrates how to use the tizen_window_manager plugin.
publish_to: "none"

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  tizen_window_manager:
    path: ../

flutter:
  uses-material-design: true
